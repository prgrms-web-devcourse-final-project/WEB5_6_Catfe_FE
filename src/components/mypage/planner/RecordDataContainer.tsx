function RecordDataContainer({ hourHeight }: { hourHeight: number }) {
  return (
    <div className="relative h-full w-1/2 bg-transparent" style={{ minHeight: hourHeight * 24 }}>
      RecordDataContainer
    </div>
  );
}
export default RecordDataContainer;
